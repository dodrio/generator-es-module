---
root: true<% if (transpile) { %>
parser: babel-eslint<% } %>
extends:
  - eslint:recommended
  - plugin:prettier/recommended<% if (!browser) { %>
  - plugin:node/recommended<% } %>
env:
  es6: true<% if (browser) { %>
  browser: true<% } else { %>
  node: true<% } %>
rules:
  semi: off
