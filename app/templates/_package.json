{
  "name": "<%= moduleName %>",
  "version": "0.0.0",
  "description": "<%= moduleDescription %>",
  "license": "MIT",
  "repository": "<%= githubUsername %>/<%= repoName %>",
  "author": {
    "name": "<%= name %>",
    "email": "<%= email %>",
    "url": "<%= humanizedWebsite %>"
  },<% if (cli) { %>
  "bin": "cli.js",<% } %>
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "test": "standard && <% if (nyc) { %>nyc <% } %>ava"
  },
  "files": [
    "index.js"<% if (cli) { %>,
    "cli.js"<% } %>
  ],
  "keywords": [<% if (cli) { %>
    "cli-app",
    "cli",<% } %>
    ""
  ],
  "dependencies": {<% if (cli) { %>
    "yargs": "^8.0.1"
  <% } %>},
  "devDependencies": {
    "ava": "^0.19.1",<% if (coveralls) { %>
    "coveralls": "^2.11.12",<% } %><% if (nyc) { %>
    "nyc": "^10.0.0",<% } %>
    "standard": "^10.0.2"
  }<% if (coveralls) { %>,
  "nyc": {
    "reporter": [
      "lcov",
      "text"
    ]
  }<% } %>
}
