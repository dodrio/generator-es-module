---
root: true<% if (browser) { %>
parser: babel-eslint<% } else { %>
parserOptions:
  sourceType: module<% } %>
extends:
  - eslint:recommended
  - plugin:prettier/recommended<% if (!browser) { %>
  - plugin:node/recommended<% } %>
env:
  es6: true<% if (browser) { %>
  browser: true<% } else { %>
  node: true<% } %>
rules:
  semi: off<% if (!browser) { %>
  # disable rules conflict with eslint-plugin-node
  node/no-unsupported-features/es-syntax: off<% } %>
